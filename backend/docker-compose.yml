services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data        # persist SQLite DB and cricsheet JSON files
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
